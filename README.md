# VibeGit Checkpoints 🌊

**Stay in the flow. Code without fear.**

Терминальный инструмент для тех, кто хочет кодить в потоке, не отвлекаясь на формальности Git. Делай мгновенные снэпшоты (вайб-чеки), экспериментируй и всегда имей возможность вернуться назад.

## Вайб-статус: Готов к флоу ✅

**Никакого стресса с коммитами. Просто сейвь вайб.**

### Почему это круто:
- 🌊 **Stay in Flow**: Интерфейс, который не выбивает из колеи.
- ⚡ **Instant Save**: Сейв состояния одной кнопкой. Не думай над `git add` и `git commit -m`.
- 🧠 **Smart Suggestions**: ИИ (или просто крутые пресеты) подскажут описание, если лень писать.
- 🛡️ **Safety Net**: Сломал всё рефакторингом? Вернись к прошлому вайбу за секунду.
- 🚀 **Sync**: Синк с удалёнкой без боли. Конфликты? Решим.

### Возможности:
- **Вайб-метр**: Визуальный статус репозитория (чисто/грязно, ветка, синхрон).
- **Снэпшоты**: Быстрые сохранения локального состояния.
- **Машина времени**: Наглядная история и мгновенный откат.
- **Zero Friction**: Управление стрелками и хоткеями (C/H/R/S).
- **Русский интерфейс**: Полная локализация для наших вайб-кодеров.

### Установка:

#### Быстрый старт (рекомендуется):
```bash
# Для Linux/macOS/Windows (через Git Bash)
curl -fsSL https://raw.githubusercontent.com/YOUR_USERNAME/git-checkpoint-app/main/install.sh | bash
```

#### Ручная установка:
1. Скачай бинарник для своей платформы из [Releases](https://github.com/YOUR_USERNAME/git-checkpoint-app/releases)
2. Распакуй архив
3. Сделай файл исполняемым: `chmod +x git-checkpoint`
4. Перемести в PATH: `sudo mv git-checkpoint /usr/local/bin/`

### Управление потоком:
```bash
# После установки просто запусти
git-checkpoint

# Или из исходников
./build.sh
go build -ldflags="-s -w" -o git-checkpoint .
./git-checkpoint
```

### Управление потоком:
- `↑` / `↓` - Выбор действия
- `Enter` - Погнали
- `C` - **C**heckpoint (Сейв)
- `H` - **H**istory (История)
- `R` - **R**ollback (Откат)
- `S` - **S**ync (Синк)

---
*Code with vibe, commit with confidence.*